[phases.setup]
apt_pkgs = ["python3-dev"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput"]

[phases.start]
cmds = ["gunicorn social_api.wsgi:application --bind 0.0.0.0:$PORT"]
